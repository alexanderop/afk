{
  "name": "afk",
  "private": true,
  "type": "module",
  "scripts": {
    "//docs": "Docs site lives in site/ (own deps). Start the VitePress dev server: `cd site && bun install && bun run docs:dev` (http://localhost:5173/afk/). Build: `bun run docs:build`. Preview: `bun run docs:preview`.",
    "test": "bun tests/check.ts",
    "test:unit": "bun tests/unit/run-unit-tests.ts",
    "test:integration": "bun tests/integration/run-integration-tests.ts",
    "test:e2e": "bun tests/e2e/plugin-load.ts",
    "test:evals": "bun tests/e2e/evals/run-evals.ts",
    "test:triggers": "bun tests/e2e/triggers/run-triggers.ts"
  }
}
