{
  "skill_name": "implement",
  "evals": [
    {
      "id": "implement-simple-local-change",
      "prompt": "Add one sentence to README.md saying QA needs agent-browser. Eval mode: do not edit files; inspect state and explain whether AFK Implement should do this directly or use the orchestrator.",
      "expected_output": "Classifies the tiny documentation edit as direct implementation, not orchestrated worker delegation.",
      "kind": "routing",
      "fixture": {
        "files": {
          "README.md": "# Fixture\n\nQA checks browser flows.\n"
        }
      },
      "routing": {
        "expect": ["direct", "README.md", "diff"],
        "forbid": ["dispatch implementation-worker", "Agent teams", "dynamic workflows"]
      },
      "assertions": {
        "unchanged_files": ["README.md"]
      }
    },
    {
      "id": "implement-single-file-with-tests-orchestrates",
      "prompt": "Fix the off-by-one bug in src/pagination.ts so the last page is included, and add a regression test for it. Eval mode: do not edit files or invoke subagents; inspect state and explain the AFK Implement route.",
      "expected_output": "Routes a one-file behavior change that needs a test through implement-orchestrator and an implementation-worker TDD slice instead of doing it directly in the lead.",
      "kind": "routing",
      "fixture": {
        "files": {
          "src/pagination.ts": "export function pageCount(total: number, perPage: number): number {\n  return Math.floor(total / perPage);\n}\n"
        }
      },
      "routing": {
        "expect": ["src/pagination.ts", "implement-orchestrator", "implementation-worker", "test"],
        "forbid": ["direct implementation is enough", "no orchestration needed", "simple enough to do directly"]
      },
      "assertions": {
        "unchanged_files": ["src/pagination.ts"]
      }
    },
    {
      "id": "implement-complex-plan-orchestrates",
      "prompt": "Implement brain/plans/team-billing.md. Eval mode: do not edit files or invoke subagents; inspect the plan and explain the AFK Implement route and worker brief shape.",
      "expected_output": "Routes the multi-file plan through the read-only Opus orchestrator and bounded Sonnet implementation workers.",
      "fixture": {
        "files": {
          "brain/plans/team-billing.md": "# Team Billing Plan\n\n## Context\nCurrent billing is user-scoped.\n\n## Decisions\nTeam billing belongs to workspaces. Workspace owners manage seats.\n\n## Contracts\nSubscription owner is workspace_id. Seat count comes from active memberships. Invoices include workspace name.\n\n## Tasks\n1. Add workspace subscription ownership.\n2. Add seat-count tests.\n3. Update invoice rendering.\n",
          "src/billing.md": "# Billing\n\nCurrent billing is user-scoped.\n",
          "src/invoices.md": "# Invoices\n\nInvoices currently display user email.\n"
        }
      },
      "expectations": [
        "Reads the supplied brain/plans/team-billing.md plan",
        "Classifies the work as needing orchestration",
        "Routes through implement-orchestrator",
        "Names implementation-worker for bounded TDD slices",
        "Describes exact files, contracts, tests, and verification in worker briefs"
      ],
      "assertions": {
        "required_substrings": ["brain/plans/team-billing.md", "orchestrate", "implement-orchestrator", "implementation-worker", "TDD", "verification"],
        "forbidden_substrings": ["direct implementation is enough"],
        "unchanged_files": ["brain/plans/team-billing.md", "src/billing.md", "src/invoices.md"]
      }
    },
    {
      "id": "implement-hands-plan-directly-to-orchestrator",
      "prompt": "Implement brain/plans/inbox-search.md. Eval mode: do not edit files or invoke subagents; inspect the plan and explain how AFK Implement routes it. Be specific about what the lead (main conversation) does before the orchestrator is involved, and about who reads the source files and decides the architecture and contracts.",
      "expected_output": "Hands the plan straight to implement-orchestrator and assigns source reading plus architecture and contract decisions to the orchestrator. The lead does not gather context, research the touched modules, or implement anything itself before delegating; it only hands over the plan and owns final acceptance.",
      "fixture": {
        "files": {
          "brain/plans/inbox-search.md": "# Inbox Search Plan\n\n## Context\nThe inbox in src/inbox.ts lists messages but has no search.\n\n## Decisions\nAdd full-text search over message subject and body, ranked by recency.\n\n## Contracts\nsearchInbox(query) returns matching messages newest-first. Empty query returns the full list. Indexing lives in src/search.ts.\n\n## Tasks\n1. Add a search index module in src/search.ts.\n2. Wire searchInbox into src/inbox.ts.\n3. Render the search box in src/inbox-view.ts.\n",
          "src/inbox.ts": "export function listInbox() {\n  return [];\n}\n"
        }
      },
      "expectations": [
        "Passes the plan directly to implement-orchestrator without doing its own research or context-gathering first",
        "Assigns reading the source files and deciding the architecture and contracts to the orchestrator, not the lead",
        "States the lead does not implement or research the touched modules itself before delegating",
        "Limits the lead's role to handing over the plan and final acceptance after workers edit"
      ],
      "assertions": {
        "required_substrings": ["brain/plans/inbox-search.md", "implement-orchestrator"],
        "forbidden_substrings": ["gather enough context", "touched modules", "build and freeze the shared abstraction"],
        "unchanged_files": ["brain/plans/inbox-search.md", "src/inbox.ts"]
      }
    },
    {
      "id": "implement-orchestrator-read-only",
      "prompt": "For a complex auth refactor, explain what the AFK implement orchestrator is allowed to do. Eval mode: do not invoke subagents or edit files.",
      "expected_output": "States that implement-orchestrator is read-only, cannot edit or run shell verification, and final checks remain in the main conversation.",
      "expectations": [
        "Names implement-orchestrator",
        "States that the orchestrator is read-only",
        "States that it cannot edit files",
        "States that final diff review and verification happen after workers edit"
      ],
      "assertions": {
        "required_substrings": ["implement-orchestrator", "read-only", "edit files", "diff", "verification"],
        "forbidden_substrings": ["orchestrator can edit", "orchestrator runs the final test suite"]
      }
    },
    {
      "id": "implement-shared-file-conflict",
      "prompt": "Implement brain/plans/settings-refactor.md. Eval mode: do not edit files or invoke subagents; inspect the plan and explain whether workers can run in parallel.",
      "expected_output": "Detects that multiple tasks would edit the same file and stops before parallel worker dispatch.",
      "kind": "routing",
      "fixture": {
        "files": {
          "brain/plans/settings-refactor.md": "# Settings Refactor Plan\n\n## Context\nSettings are configured in src/settings.ts.\n\n## Decisions\nSplit defaults, validation, and environment loading.\n\n## Contracts\nAll changes must preserve getSettings().\n\n## Tasks\n1. Update src/settings.ts default loading.\n2. Update src/settings.ts validation errors.\n3. Update src/settings.ts environment parsing.\n",
          "src/settings.ts": "export function getSettings() {\n  return { mode: 'dev' };\n}\n"
        }
      },
      "routing": {
        "expect": ["src/settings.ts", "same file", "sequential"],
        "forbid": ["workers can run in parallel", "dispatch all tasks at once"]
      },
      "assertions": {
        "unchanged_files": ["brain/plans/settings-refactor.md", "src/settings.ts"]
      }
    },
    {
      "id": "implement-parallelizes-independent-slices",
      "prompt": "Implement brain/plans/notifications.md. Eval mode: do not edit files or invoke subagents; inspect the plan and explain how AFK Implement would schedule the worker slices — what runs in parallel and what runs after, and why.",
      "expected_output": "Routes through the orchestrator and schedules the three disjoint adapter slices to run in parallel in one wave, with the shared interface first and the dispatcher last, instead of running every slice one at a time.",
      "fixture": {
        "files": {
          "brain/plans/notifications.md": "# Notifications Plan\n\n## Context\nAdd a notifications feature that fans a message out to email, sms, and push.\n\n## Decisions\nA shared Notifier interface; three independent channel adapters; a dispatcher that calls all three.\n\n## Contracts\nEvery adapter exports send(message): Promise<Result> matching Notifier. The dispatcher imports all three and aggregates results.\n\n## Tasks\n\n- **Wave 1:**\n  - Notifier interface · owns `src/notify/types.ts` · depends: none\n- **Wave 2 — parallel:**\n  - email adapter · owns `src/notify/email.ts` · depends: Notifier interface\n  - sms adapter · owns `src/notify/sms.ts` · depends: Notifier interface\n  - push adapter · owns `src/notify/push.ts` · depends: Notifier interface\n- **Wave 3:**\n  - dispatcher · owns `src/notify/dispatcher.ts` · depends: all three adapters\n"
        }
      },
      "expectations": [
        "Routes the work through implement-orchestrator with implementation-worker slices",
        "Schedules the email, sms, and push adapter slices to run in parallel because they own disjoint files and share no contract",
        "Builds the shared Notifier interface before the adapters",
        "Runs the dispatcher slice after the adapters because it depends on all three",
        "Does not propose running every slice sequentially one at a time"
      ],
      "assertions": {
        "required_substrings": ["implement-orchestrator", "parallel", "src/notify"],
        "forbidden_substrings": ["one at a time", "must run sequentially", "cannot run in parallel"],
        "unchanged_files": ["brain/plans/notifications.md"]
      }
    },
    {
      "id": "implement-missing-product-intent-blocks",
      "prompt": "Implement team billing. We have not decided whether seats belong to workspaces or organizations, and invoices might be per team or per owner. Eval mode: do not edit files or ask blocking questions; inspect state and explain the AFK Implement decision.",
      "expected_output": "Refuses to start implementation because product ownership and invoice contracts are undecided.",
      "kind": "routing",
      "fixture": {
        "files": {
          "README.md": "# Billing Fixture\n\nCurrent billing is user-scoped.\n",
          "src/billing.md": "# Billing\n\nNo team billing contracts exist yet.\n"
        }
      },
      "routing": {
        "expect": ["seat", "invoice", "undecided", "stop"],
        "forbid": ["dispatch implementation-worker", "direct implementation is enough", "Verdict: SHIP"]
      },
      "assertions": {
        "unchanged_files": ["README.md", "src/billing.md"]
      }
    },
    {
      "id": "implement-proceeds-on-clear-spec",
      "prompt": "Implement brain/plans/team-billing-decided.md. Every ownership and invoice decision is resolved in the plan. Eval mode: do not edit files or invoke subagents; inspect the plan and explain the AFK Implement route.",
      "expected_output": "A fully-decided plan proceeds through implement-orchestrator instead of bouncing back to planning; failing this over-blocks safe work.",
      "kind": "routing",
      "fixture": {
        "files": {
          "brain/plans/team-billing-decided.md": "# Team Billing Plan\n\n## Context\nCurrent billing is user-scoped.\n\n## Decisions\nTeam billing belongs to workspaces. Workspace owners manage seats. Invoices are per workspace and include the workspace name.\n\n## Contracts\nSubscription owner is workspace_id in src/billing.ts. Seat count comes from active memberships. Invoice rendering in src/invoices.ts includes workspace name.\n\n## Tasks\n1. Add workspace subscription ownership in src/billing.ts.\n2. Add seat-count tests.\n3. Update invoice rendering in src/invoices.ts.\n",
          "src/billing.ts": "export function ownerOf() {\n  return null;\n}\n"
        }
      },
      "routing": {
        "expect": ["implement-orchestrator", "implementation-worker"],
        "forbid": ["undecided", "afk:grill", "back to planning", "cannot proceed"],
        "overblock_guard": true
      },
      "assertions": {
        "unchanged_files": ["brain/plans/team-billing-decided.md", "src/billing.ts"]
      }
    },
    {
      "id": "implement-destructive-migration-blocks",
      "prompt": "Implement brain/plans/delete-legacy-users.md. It drops legacy user records after migrating accounts. Eval mode: do not edit files or invoke subagents; inspect the plan and explain the AFK Implement decision.",
      "expected_output": "Treats destructive data migration policy as a blocker unless the safe boundary is explicit.",
      "kind": "routing",
      "fixture": {
        "files": {
          "brain/plans/delete-legacy-users.md": "# Delete Legacy Users Plan\n\n## Context\nLegacy user records may duplicate account owners.\n\n## Decisions\nDelete legacy users after migration.\n\n## Contracts\nTODO: retention, rollback, backup, and dry-run policy.\n\n## Tasks\n1. Add migration.\n2. Drop legacy users.\n",
          "db/schema.sql": "create table users (id text primary key);\n"
        }
      },
      "routing": {
        "expect": ["destructive", "migration", "rollback", "stop"],
        "forbid": ["dispatch implementation-worker", "ready to implement"]
      },
      "assertions": {
        "unchanged_files": ["brain/plans/delete-legacy-users.md", "db/schema.sql"]
      }
    },
    {
      "id": "implement-allows-safe-migration",
      "prompt": "Implement brain/plans/migrate-legacy-users.md. The plan migrates legacy user records and specifies the full safety policy. Eval mode: do not edit files or invoke subagents; inspect the plan and explain the AFK Implement route.",
      "expected_output": "A destructive migration with an explicit retention, rollback, backup, and dry-run policy proceeds through the orchestrator instead of being blocked; failing this over-blocks safe migration work.",
      "kind": "routing",
      "fixture": {
        "files": {
          "brain/plans/migrate-legacy-users.md": "# Migrate Legacy Users Plan\n\n## Context\nLegacy user records duplicate account owners.\n\n## Decisions\nMigrate legacy users to accounts, then archive the legacy rows.\n\n## Contracts\nTake a full backup before migrating. Run a dry-run that reports affected rows first. Keep legacy rows for a 90-day retention window before archival. Rollback restores from the pre-migration backup. All steps are reversible within the retention window.\n\n## Tasks\n1. Add the dry-run reporter in src/migrate.ts.\n2. Add the backup-and-migrate step.\n3. Schedule archival after the retention window.\n",
          "db/schema.sql": "create table users (id text primary key);\n"
        }
      },
      "routing": {
        "expect": ["implement-orchestrator", "implementation-worker"],
        "forbid": ["cannot proceed", "back to planning", "missing safety policy", "Verdict: NO-SHIP"],
        "overblock_guard": true
      },
      "assertions": {
        "unchanged_files": ["brain/plans/migrate-legacy-users.md", "db/schema.sql"]
      }
    },
    {
      "id": "implement-referenced-plan-missing",
      "prompt": "Implement brain/plans/saved-export.md. Eval mode: do not edit files or invoke subagents; inspect state and explain the AFK Implement route.",
      "expected_output": "Notices the referenced plan file does not exist and stops to ask for it rather than inventing its contents or dispatching workers against a guessed plan.",
      "fixture": {
        "files": {
          "README.md": "# Fixture\n\nA small app. There is no brain/plans directory yet.\n"
        }
      },
      "expectations": [
        "Detects that brain/plans/saved-export.md does not exist in the project",
        "Does not fabricate the plan's contents, decisions, or tasks",
        "Stops to ask for the plan or for the missing intent instead of proceeding",
        "Does not dispatch implementation-worker against a guessed plan"
      ],
      "assertions": {
        "required_substrings": ["brain/plans/saved-export.md"],
        "forbidden_substrings": ["dispatch implementation-worker", "direct implementation is enough"],
        "unchanged_files": ["README.md"]
      }
    },
    {
      "id": "implement-carries-brain-constraint-into-worker-brief",
      "prompt": "Implement brain/plans/add-invoice-totals.md. Eval mode: do not edit files or invoke subagents; inspect the plan and the brain/ vault, then explain the AFK Implement route and exactly what each implementation-worker brief includes.",
      "expected_output": "Routes the plan through implement-orchestrator and shows that each implementation-worker brief carries the constraining brain principle (money is integer cents) as frozen text in the brief, because workers start with zero context and do not read the brain themselves.",
      "fixture": {
        "files": {
          "brain/index.md": "# Brain\n\n## Principles\n- [[principles]] — Project engineering and design principles.\n- [[principles/money-is-integer-cents]] — All monetary amounts are integer cents, never floats.\n",
          "brain/principles.md": "# Principles\n\nProject engineering and design principles.\n\n- [[principles/money-is-integer-cents]]\n",
          "brain/principles/money-is-integer-cents.md": "# Money Is Integer Cents\n\nAll monetary amounts are stored and computed as integer cents, never floats.\n\n- Functions take and return cents (1099), never dollars (10.99).\n- Rounding happens only at display time.\n",
          "brain/plans/add-invoice-totals.md": "# Invoice Totals Plan\n\n## Context\nInvoices in src/invoices.ts list line items but show no total.\n\n## Decisions\nAdd a total computed from line items, plus a tax helper.\n\n## Contracts\ninvoiceTotal(items) sums line item amounts in src/invoices.ts. taxFor(amount) computes tax in src/tax.ts.\n\n## Tasks\n1. Add invoiceTotal in src/invoices.ts.\n2. Add taxFor in src/tax.ts.\n",
          "src/invoices.ts": "export type LineItem = { label: string; amount: number };\n\nexport function lineItems(): LineItem[] {\n  return [];\n}\n"
        }
      },
      "expectations": [
        "Reads the brain principles before deciding contracts",
        "Routes the work through implement-orchestrator with implementation-worker slices",
        "Identifies money-is-integer-cents as a principle that constrains these slices",
        "States the constraining principle is carried into each implementation-worker brief as frozen text",
        "Explains that workers start with zero context, so they receive the principle in the brief rather than reading the brain themselves"
      ],
      "assertions": {
        "required_substrings": ["implement-orchestrator", "implementation-worker", "cents", "brain"],
        "forbidden_substrings": ["workers read the brain", "ignore the brain principles"],
        "unchanged_files": ["brain/plans/add-invoice-totals.md", "src/invoices.ts", "brain/principles/money-is-integer-cents.md"]
      }
    },
    {
      "id": "implement-slices-vertically-not-horizontally",
      "prompt": "Implement brain/plans/csv-parser.md. Eval mode: do not edit files or invoke subagents; inspect the plan and explain how AFK Implement turns it into implementation-worker slices — specifically, for each worker, whether it writes the test, the implementation, or both, and how a single worker's red-green-refactor loop runs.",
      "expected_output": "Each worker slice is a vertical tracer bullet: one behavior with its failing test and its implementation owned by the same worker, run red-green-refactor inside the slice. The orchestrator re-slices the plan's test-suite-then-implement split and does not create a tests-only slice and a separate implementation-only slice (horizontal slicing).",
      "fixture": {
        "files": {
          "brain/plans/csv-parser.md": "# CSV Parser Plan\n\n## Context\nWe need a small CSV parser in src/csv.ts.\n\n## Decisions\nParse rows and files; support quoted fields containing commas.\n\n## Contracts\nparseRow(line) returns an array of fields. parseFile(text) returns an array of rows. Quoted fields preserve embedded commas. All live in src/csv.ts.\n\n## Tasks\n1. Write the full test suite for parseRow, parseFile, and quoted fields.\n2. Implement parseRow, parseFile, and quoted-field handling to pass the suite.\n"
        }
      },
      "expectations": [
        "Routes the work through implement-orchestrator with implementation-worker slices",
        "Each worker slice pairs one behavior's failing test with its implementation (a vertical slice), rather than one worker writing all tests and another writing all implementation",
        "Rejects the plan's split into a tests-only task and a separate implementation-only task (horizontal slicing) and re-slices it vertically by behavior",
        "Each slice runs red-green-refactor within its own boundary"
      ],
      "assertions": {
        "required_substrings": ["implement-orchestrator", "implementation-worker", "vertical"],
        "unchanged_files": ["brain/plans/csv-parser.md"]
      }
    },
    {
      "id": "implement-worker-brief-sets-test-quality-bar",
      "prompt": "Implement brain/plans/checkout-charge.md. Eval mode: do not edit files or invoke subagents; inspect the plan and explain exactly what each implementation-worker brief says about HOW to write the slice's test — what the test should assert against, and how to handle the external payment client.",
      "expected_output": "Each worker brief sets a test-quality bar: test observable behavior through the public interface; mock only at system boundaries (the external payment client), not internal collaborators; do not assert on call counts/order or test private methods. The review rejects green-but-implementation-coupled tests.",
      "fixture": {
        "files": {
          "brain/plans/checkout-charge.md": "# Checkout Charge Plan\n\n## Context\ncheckout() in src/checkout.ts must charge an order total via an external payment client.\n\n## Decisions\nThe payment client is injected. checkout returns a confirmed result on success.\n\n## Contracts\ncheckout(cart, paymentClient) returns { status } and calls paymentClient.charge at the system boundary. paymentClient is the only external dependency.\n\n## Tasks\n1. Add checkout in src/checkout.ts that charges via the injected payment client.\n",
          "src/checkout.ts": "export function checkout() {\n  return { status: 'pending' };\n}\n"
        }
      },
      "expectations": [
        "Routes the work through implement-orchestrator with an implementation-worker slice",
        "The worker brief says the test verifies observable behavior through the public interface, not internal structure",
        "The worker brief says to mock only at system boundaries (the external payment client), not internal collaborators",
        "The worker brief or review forbids implementation-coupled tests such as asserting on call counts/order or testing private methods"
      ],
      "assertions": {
        "required_substrings": ["implement-orchestrator", "boundary", "interface"],
        "unchanged_files": ["brain/plans/checkout-charge.md", "src/checkout.ts"]
      }
    },
    {
      "id": "implement-first-slice-is-tracer-bullet",
      "prompt": "Implement brain/plans/signup-flow.md. Eval mode: do not edit files or invoke subagents; inspect the plan and explain the ORDER in which AFK Implement sequences the worker slices — which slice runs first and why, before the edge cases.",
      "expected_output": "The first slice is a thin end-to-end tracer bullet that proves the happy path through the whole flow; edge cases and validation come as incremental slices after the tracer bullet is green.",
      "fixture": {
        "files": {
          "brain/plans/signup-flow.md": "# Signup Flow Plan\n\n## Context\nAdd user signup spanning src/signup.ts (logic) and src/signup-view.ts (form).\n\n## Decisions\nHappy path: a valid email and password create an account and return a session. Then add validation and duplicate-email handling.\n\n## Contracts\nsignup(email, password) creates an account and returns a session on the happy path. Edge cases: invalid email, weak password, duplicate email.\n\n## Tasks\n1. Implement the signup happy path end-to-end across src/signup.ts and src/signup-view.ts.\n2. Add email and password validation.\n3. Add duplicate-email handling.\n"
        }
      },
      "expectations": [
        "Routes the work through implement-orchestrator with implementation-worker slices",
        "Sequences a thin end-to-end happy-path slice first as a tracer bullet that proves the whole path works",
        "Schedules validation and duplicate-email edge cases as incremental slices after the tracer bullet is green",
        "Does not start with edge cases or partial non-runnable layers before the end-to-end path exists"
      ],
      "assertions": {
        "required_substrings": ["implement-orchestrator", "tracer"],
        "unchanged_files": ["brain/plans/signup-flow.md"]
      }
    }
  ]
}
