# AFK Brain Index

- [[native-worktree-isolation]] — Why ralph uses manual `git worktree add -b`, not the harness's `isolation: worktree` — native worktrees branch from the 
- [[shellcheck-invocation]] — Why lint runs `shellcheck -x -P SCRIPTDIR` — plain shellcheck false-fails on SC1091. Read before changing the lint comma
